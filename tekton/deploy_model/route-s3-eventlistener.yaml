apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deploy-model
spec:
  rules:
  - http:
      paths:
      - path: /deploy-model
        pathType: Prefix
        backend:
          service:
            name: el-deploy-model-listener
            port:
              number: 8080
