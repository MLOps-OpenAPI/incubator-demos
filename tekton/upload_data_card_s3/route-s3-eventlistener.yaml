apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: data-intake
spec:
  rules:
  - http:
      paths:
      - path: /data-intake
        pathType: Prefix
        backend:
          service:
            name: el-s3-listener
            port:
              number: 8080