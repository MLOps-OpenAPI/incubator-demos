apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: build-model
spec:
  rules:
  - http:
      paths:
      - path: /build-model
        pathType: Prefix
        backend:
          service:
            name: el-build-model-listener
            port:
              number: 8080