apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: request-model
spec:
  rules:
  - http:
      paths:
      - path: /request-model
        pathType: Prefix
        backend:
          service:
            name: el-request-model-listener
            port:
              number: 8080