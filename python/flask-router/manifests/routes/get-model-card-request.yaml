apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: get-model-card-request
  namespace: demo
spec:
  rules:
  - http:
      paths:
      - path: /get-model-card-request
        pathType: Prefix
        backend:
          service:
            name: flask-router
            port:
              number: 8081