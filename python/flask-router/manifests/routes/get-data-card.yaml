apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: get-data-card
  namespace: demo
spec:
  rules:
  - http:
      paths:
      - path: /get-data-card
        pathType: Prefix
        backend:
          service:
            name: flask-router
            port:
              number: 8081